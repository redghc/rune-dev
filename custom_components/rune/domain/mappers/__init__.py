"""Mappers subpackage — domain-level translations between representations.

- ``speed_mapper`` — % ⇄ discrete N-steps ⇄ named presets.
- ``climate_mapper`` — climate state lattice mapping (lives in Phase 1
  follow-up if needed; placeholder for now).
- ``action_mapper`` — signal → action dispatch logic (lives here once
  triggers/engine.py depends on it; placeholder for now).
"""
